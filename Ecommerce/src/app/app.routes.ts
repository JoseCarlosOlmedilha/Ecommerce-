import { Routes } from '@angular/router';
import { UsuarioCadastro } from './components/usuario-cadastro/usuario-cadastro';

export const routes: Routes = [
    {path: "usuario", component: UsuarioCadastro}
];

